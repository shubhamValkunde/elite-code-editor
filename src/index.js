import React from "react";
import ReactDOM from "react-dom";
import SimpleEditor from "./App.jsx"; // Import the default export

ReactDOM.render(
  <React.StrictMode>
    <SimpleEditor />
  </React.StrictMode>,
  document.getElementById("root")
);
